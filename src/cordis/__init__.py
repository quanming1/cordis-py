"""cordis-py —— Cordis 的 Python 移植（时空可组合性元框架）。

移植自 https://github.com/cordiverse/cordis （TypeScript）。
核心语义：可撤销副作用（时间维度）+ 响应式依赖注入（空间维度）。

当前状态：A 阶段（地基），功能模块将在 B+ 阶段逐步落地（见 docs/TODO.yaml）。
"""

__version__ = "0.1.0"
